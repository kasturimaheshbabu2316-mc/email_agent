# Phase 0 – Bootstrap

This folder contains the initial scaffolding required to get the project running.

- **README.md** – Quick‑start guide.
- **.env.example** – Template for environment variables.
- **requirements.txt** – Python dependencies (including Groq SDK).

Copy `.env.example` to `.env` and fill in your credentials before running the code.
